version: '3.8'

services:
  cloudmed-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cloudmed_healthcare_system
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: always
